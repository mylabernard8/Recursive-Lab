function setup() {
  createCanvas(400, 400); 
  noFill();               
  noLoop();               
}

function draw() {
  background(220);        
  rSquare(x, y, size); //recursive code (have to finish code later)
}

function rSquare(x, y, size) {
  if (/* stop recursive function */) {
    return;
  }

  // draw in square (have to flush out later)

  // make recursive formula for smaller shapes
}
